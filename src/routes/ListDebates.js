import React, { Component } from 'react';
import { TouchableOpacity, Text, StyleSheet, View } from 'react-native';

const ListDebates = () => {
    return (
        <View>
            <Text>ListDebates</Text>
        </View>
    );
};

export default ListDebates;
